<h1>Игры</h1>

{{#if games.length }}
  {{#each games}}
    <div class='row'>
      <div class='col s6 offset-s3'>
        <div class='card'>
          <div class='card-image'>
            <img src='{{ image }}' alt='{{ title }}'>
          </div>
          <div class='card-content'>
            <span class='card-title'>{{ title }}</span>
            <p class='price'>{{ price }}</p>
          </div>
          <div class='card-action'>
            <a href='/games/{{ id }}' target='_blank'>Посмотреть игру</a>
            <a href='/games/{{ id }}/edit?allow=true'>Редактировать</a>
          </div>
        </div>
      </div>
    </div>
  {{/each}}
{{else}}
<p>Игр пока нет</p>
{{/if}}